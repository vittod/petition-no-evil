<div class="introduction">
    <h1>we petition against anything!</h1>
    <h2>please login to sign the petition</h2>
</div>

<div class="content">
    <form class="" action="/login/" method="post" enctype="application/x-www-form-urlencoded">
        <input type="email" name="email">
        <label for="email">your email *</label>
        <input type="password" name="pass">
        <label for="pass">your password *</label>
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
        <button type="submit" name="register-submit"><i class="fas fa-sign-in-alt"></i> login</button>
    </form>
    <p class="message">{{msg}}</p>
    <p>or would you like to register? <a href="/register">go to register page</a> </p>
</div>

{{> deco_2}}
